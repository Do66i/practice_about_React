```js

import { useState } from "react";

const heavyWork = () => {
  // 초기 값을 얻기 위한 함수
  console.log("엄청 무거운 작업!!!");
  return ["홍길동", "김민수"];
};

function App() {
  const [names, setNames] = useState(() => heavyWork());
  heavyWork()
  값을 저장하지만 그 함수를 계속 호출함

  () => heavyWork() 초기값 가져와야할때 함수 호출해야한다면 무조건 이렇게
  초기에 한번만 실행이 되서 더이상 실행되지 않음

  const [input, setInput] = useState("");

  const handleInputChange = (e) => {
    setInput(e.target.value);
  };

  const handleUpload = () => {
    setNames((prevState) => {
      console.log("이전 state: ", prevState);
      return [input, ...prevState];
    });
  };

  return (
    <div>
      <input type="text" value={input} onChange={handleInputChange} />
      <button onClick={handleUpload}>Upload</button>
      {names.map((name, idx) => {
        return <p key={idx}>{name}</p>;
      })}
    </div>
  );
}

export default App;

```

출처 : 별코딩, 현민님

1일 1커밋

1차 피드백 정리못함

```
피드백
-> 위키 홈
마케팅(언어)부분은 최대한 짧게 = 이 프로젝트가 어떤문제를 해결하기 위해 만들어진 것인지 최대한 간략하게
기능들은 최대한 길고 섬세하게 !
- 우리팀 주요 기능은 좀 더 직관적으로 쓰기
- 1일 1로그는 매우 좋은자세 !
- Dev Log 는 안볼수도 있다 그러나, 이것을 써놓으면 프로젝트 끝나고 회고를 쓸 때 참고하여 쓸 수 있기 때문 + 실제로 보는 회사도 있다 (이 분이 이 프로젝트에서 어떤것들을 기여했나 알 수 있다)
- 에러로그도 쓰면 “스토리”가 생김 (회고 시)
- ??? : 이력서에 블로그랑 깃허브만 본다 << 이런사람도 있음
- 스토리 중요함 (에러를 어떻게 “다같이” 해결해 나가는지) <<
- 형식은 자유 ( 이슈 + 개인블로그 ) << 모범답안
- 프론트엔드는 특히 프로토타입에 거의 몰두해도 무방하다 (플로우 차트도 넣을수잇음)
- 프로토타입은 실제 서비스와 아주 유사하게 만든다 ( 만들때 일렬로 만들어도 무방)
- 과도할정도로 세부하게 만들어도 된다.
- 만약 모바일버전도 할 거면 모바일 프로토타입도 필요함
- 요구사항은 베어/어드/나이트 나누고 명쾌하고 상세하게 !
- 4F 는 매일써도된다…. 기록이 매우매우 중요하다
- DB 스키마 보고 크루님 느낌 : 하나의 trip에 여러명이 ? 1:N?이 맞는가
- DB는 정말 엄밀하게 짜야함 왜? 나중에 수정하기 매우매우매우 어렵다
- Paid_person(네이밍을 더 신경써야함) : 이게 굳이 필요한가 ? 이걸 빼고 user.nickname 이 어떤지 (왜냐하면 스키마에서 다 연결이 되어있음)
- 돈 지출내역만 있는지 ? 수입내역은 ?
- 소비를 하는거면.. 네이밍을 다시하는걸로 Account 라는 네이밍을 다시 짓는걸 권유
- 유저들끼리 소통하는 공간이 왜 없지 ? 웹페이지인데 ?
- 컨셉이 개인기록용이면 빼는것도 좋음
- 하지만 이런 프로젝트엔 소통하는 공간이 대부분 들어가잇음
- 중요한 것 : 프로젝트 만드는 과정에서 자기자신에게 도움이 되어야하는데 너무 결과물에 스트레스를..안받을 수는 없지만 … 다음 스몰톡부터는 확인하는 과정을 가질예정
- 🚨깃허브에 들어갔을때 (담당자 인 경우) 1. ⭐️API문서부터 확인⭐️ ( API문서는 개발자의 꽃이다. API를 대하는 것만 봐도 실력이 보임 = API를 정말정말정말정말 잘 짜야함 ex> REST API )
- API - ☀️라우터 분리, /sigh /trip으로 나눌 수 있는데 /sign/up /sign/in… 이렇게 나누는건 어떤지. 500에러는 빼도됨 ( 메세지도 굳이 쓸 필요없음 하지만 메세지에 id넣은것은 good). 여행정보 요청부분도 /trip/:id 이렇게 간결하게 써도 괜찮을 것 같다. 실제 데이터처럼 적은것은 매우 잘한 부분. Trip 기간 설정? -> 생성이라고 수정을 해야할 것 같음. 삭제부분은 잘해주셨음. 유저정보변경같은 경우 (/mypage 보단 /user 이 맞는 표현). 회원탈퇴 (/withdrwal << 이건 행위니까 DELETE/user 처럼 바꿔주는 것이 좋다).  /trip/:trip_id/account보단  /account 로 통일하는것이 좋다 (ex> GET/account). 금액하나변경 -> PATCH/account/:id 이렇게 변경하는것이 좋은 것 같음.
- DB스키마와 API는 용어를 맞춰줘야함.
- Hello world 까지 배포먼저해보기
- 배포자동화는 하면 좋다…. 편함…. 여력이 된다면 하는거 추천
- 완성에 모든걸 총력..을기운다
리콰이어 후 이슈테스크작성 << 한번에 다같이 !
목표를 다같이 세우기
일단 프젝끝나면 바로 취업취업취업
하나만 잘해도 뭘해도 제대로하는사람이라고 인식이됨
결론 : 리엑트나 잘하자
취업은 타이밍이다! 걍 무조건 들이박아야함
일단 프로젝트를 만든다 걍 프젝만듬 + 취업
코딩테스트 ? 어느정도 레벨이 있는지 확인하는것.
정보처리기사 ? 필요없어; 쓸데없는 공부 하지마라..
걍 어떤결과물을 주어진시간내에 만들수잇느냐 걍 결과물만들어라
결 과 물 결 과 물 결 과 물 결 과 물 !
```
